title: Making Profile component dynamic based on configuration
description: # Choosing profile view based on npLinkType got from configuration
 # Making components take configuration from configuration store rather than props
