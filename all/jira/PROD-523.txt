title: create script to remove sniped sessions
description: Oracle has profile setting which limit session idle time, but after timeout, session becomes "sniped" but not removed. Script will run every 5 minutes to clean out sniped sessions to lower the connection count to prod oracle db
