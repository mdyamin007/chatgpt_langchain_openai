title: Hypercube2  - Component - EnoticeDocument Design
description: this will take the notices type of statements based on the client configuration load the notices in year wise tab, each unviewed notice will also be marked with a red dot, and based on configuration the following headings will be supported (*Dates*, *Type*)

based on data the following button will be shown*View PDF*,*V**iew HTML*

this component will also take*stmtType*as a prop to render the statements.



this will go under /src/component/statements/eNotice
