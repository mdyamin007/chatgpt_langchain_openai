title: Hypercube - Hide caret icon from account selection for single account
description: Hide account caret icon for a single account.

Also, show account type desc concurrently both on account selector and drop-down list based on "acctTypeName" class and by default this class contain display inline.

But if any client wants to hide account type desc then they can just hide that by changing that class CSS.
