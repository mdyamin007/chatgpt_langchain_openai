title: Wrapper Service Integration
description: Setup new Wrapper Service to replace existing AFP2PDF for AFP2WEB conversion.

This will allow us to do load balancing and also consolidate the code so that all existing eStatement functionality use this same service.
 * AFP2WEB Upgrade to new version
 * Setup and configure wrapper service
 * Update all applications to point to new wrapper service.
