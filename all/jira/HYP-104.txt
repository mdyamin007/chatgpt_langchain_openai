title: Frontend - AccountPreference Edit component
description: * Single edit will extend inline. It is okay if the expansion makes the row uneven.
 * Single edit will have a cancel button that will reset the value and change the card back to view mode.
 * Submit all will have a reset button to reset the specific card, but not change back to view mode.



Edit Mode:
 * Selected option will be highlighted
 * Extra verbiage, e.g. "I agree to...", if configured, will be shown under DELIVERY TYPE.
