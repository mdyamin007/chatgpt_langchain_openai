title: ISD - SCRUM Standard Work Point Estimate
body:   

  
| Name| Description| Estimate  
---|---|---|---  
1| App Setup (New Prefix)|

  * All tasks required of a "Resource Upload"
  * Update PUI to be able to process a new prefix under an existing statement type
  * Add client file to file transfer status on PUI

| 1  
2| App Setup (New StmtType)|

  * All tasks required of a "Resource Upload"
  * Update Admin/eStatement Site
  * Update PUI to be able to process a new statement type
  * Enable email if required
  * Enable selective paperless if required
  * Setup paperless file if required
  * Add client file to file transfer status on PUI

| 3  
3| Discontinuation (w/o Deconversion)|

  * Remove estatement site access
  * Remove from PUI code and PUI/FTP configurations
  * Remove data (input, pv_nas, peb3)
  * Remove from API (if necessary)
  * Remove from SMS (if necessary)
  * Swift (if necessary)

| 3  
4| Discontinuation (w Deconversion)|

  * Remove estatement site access
  * Remove from PUI code and PUI/FTP configurations
  * Remove data (input, pv_nas, peb3)
  * Remove from API (if necessary)
  * Remove from SMS (if necessary)
  * Swift (if necessary)
  * Zip up all statement files

| 8  
5| Enotice Setup (New)|

  * All tasks required of a "Resource Upload"
  * Enable enotice display for eStatement Site
  * Enable enotice display for Admin Site
  * Enable email if necessary
  * Add description mapping for letter codes

| 3  
6| Enotice Setup (Add)|

  * All tasks required of a "Resource Upload"
  * Add description mapping for letter codes

| 1  
7| Etax Setup (New)|

  * All tasks required of a "Resource Upload"
  * Enable etax display for eStatement Site 
  * Enable etax display for Admin Site 
  * Enable email if necessary
  * Add description mapping for etax codes
  * Validate account numbers with name

| 5  
8| Etax Setup (New Form for Client)|

  * Validate account numbers with name

| 1  
9| New PUI Process (No Enhancement)|

  * Existing process that can be configured to run on client's data file

| 3  
10| New PUI Report|

  * Existing reports that can be configured using Report Scheduler

| 1  
11| PUI Configuration|

  * Update existing configuration to meet client's request, e.g. turn off email for loan

| 0.5  
12| Resource Upload|

  * Upload resource to input servers
  * Validate updated/new eStatement
  * Provide support for emergent errors

| 0.5  
13| Verbiage/UI Change|

  * Verbiage and/or UI on Admin/eStatement Site

| 1  
  
  


